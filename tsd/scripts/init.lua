-- TSD scripts init.lua
-- Runs automatically in every viewer Lua context (Scripts menu and Terminal).
-- Use this to augment the tsd.viewer table with shared helpers.

if not tsd.viewer then return end

